apiVersion: v1
kind: Service
metadata:
  name: tempconv-frontend
  namespace: default
  labels:
    app: tempconv-frontend
spec:
  type: ClusterIP
  selector:
    app: tempconv-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
